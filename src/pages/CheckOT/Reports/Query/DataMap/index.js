import React from 'react';
import {
    withStyles
} from '@material-ui/core';

const styles = theme => ({

});

class DataMap extends React.PureComponent {
    render() {

        return (
            <div>
                
            </div>
        );
    }
};

export default withStyles(styles)(DataMap);
